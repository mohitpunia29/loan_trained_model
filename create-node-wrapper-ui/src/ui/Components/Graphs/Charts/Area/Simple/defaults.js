import stackedDefaults from '../Stacked/defaults';

export default {
  chartData: {
    data : stackedDefaults.chartData.data,
    xAxis: stackedDefaults.chartData.xAxis,
    yAxis: stackedDefaults.chartData.yAxis[0]
  }
};
